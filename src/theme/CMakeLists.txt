cmake_minimum_required(VERSION 2.6)

IF( RELEASE )
install(DIRECTORY ${CMAKE_SOURCE_DIR}/src/theme DESTINATION
    ${THEMES_ENTRY_PATH} FILES_MATCHING PATTERN "*.png"
    PATTERN "CMakeFiles" EXCLUDE)
ENDIF( RELEASE )
